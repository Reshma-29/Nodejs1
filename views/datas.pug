extends layout

block content

	.row
		.card
			.card-content
				span.card-title Saved Data.!
				p Name : #{data.firstName}
				p LastName : #{data.lastName}
				p Mail : #{data.mail}
				p Age : #{data.age}

